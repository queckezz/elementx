language: node_js
sudo: false
node_js:
  - "6"
  - "7"
install:
  - npm install
  - npm install -g codecov
script:
  - npm run lint
  - npm test
  - nyc report --reporter=lcov > coverage.lcov && codecov